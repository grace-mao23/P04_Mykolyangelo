type Query {
  country_with_code(_country_code: String!): Country
}

type Country {
  id: ID!
  countryCode: String!
  countryName: String!
}

type CO2Emission {
  id: ID!
  country: Country
  time: String!
  amount: Float!
}
